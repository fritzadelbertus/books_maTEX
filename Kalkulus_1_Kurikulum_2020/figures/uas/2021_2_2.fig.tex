\begin{center}
\begin{tikzpicture}
\begin{axis}[view={30}{30},colormap/Spectral-9]
    \begin{scope}
        \def\rx{(0)}
        \def\fy{(1)}
        \addplot3[surf,shader=flat,
            samples=20,
            domain=0:pi/4,y domain=0:2*pi,
            z buffer=sort]
            ({(((\fy-\rx) * cos(deg(y)))+\rx)}, {(((\fy-\rx) * sin(deg(y)))+\rx)}, {x});
    \end{scope}

    \begin{scope}
        \def\rx{(0)}
        \def\fy{(tan(deg(x)))}
        \addplot3[surf,shader=flat,
            samples=20,
            domain=0:pi/4,y domain=0:2*pi,
            opacity=0.25,
            z buffer=sort]
            ({(((\fy-\rx) * cos(deg(y)))+\rx)}, {(((\fy-\rx) * sin(deg(y)))+\rx)}, {x});
    \end{scope}

    \addplot3[black,shader=flat,
        samples=25,
        domain=0:pi/4,
        z buffer=sort]
        (0,0,x);
    \node at (0,0.2,0.7) {$y$};
\end{axis}
\end{tikzpicture}       
\end{center}